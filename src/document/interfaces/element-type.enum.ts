/** Discriminant for every node that can appear in a Section's element list. */
export enum ElementType {
  Text = 'text',
  Divider = 'divider',
  Table = 'table',
  Image = 'image',
  QrCode = 'qrcode',
  Barcode = 'barcode',
  Feed = 'feed',
  Cut = 'cut',
  Drawer = 'drawer',
}
